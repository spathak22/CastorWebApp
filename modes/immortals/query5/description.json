{
  "Description": "Select query with join and where clause condition",
  "ExpectedQuery": " select ce.id, ce.cot_type, ce.servertime from source s join cot_event ce on s.id = ce.source_id where channel = 7"
}